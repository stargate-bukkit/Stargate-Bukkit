CREATE_TABLE_POSITION_TYPE=CREATE TABLE IF NOT EXISTS {PositionType} (id INTEGER PRIMARY KEY AUTO_INCREMENT, positionName NVARCHAR(16));
CREATE_TABLE_PORTAL_FLAG=CREATE TABLE IF NOT EXISTS {Flag} (id INTEGER PRIMARY KEY AUTO_INCREMENT, `character` CHAR(1) UNIQUE NOT NULL);